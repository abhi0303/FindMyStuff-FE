# Production builds talk to the deployed API directly — there is no Vite proxy
# once the app is built, so the origin has to be baked in.
# The API's CORS_ORIGINS must include whatever origin you host this bundle on.
VITE_API_BASE_URL=https://findmystuff-api-b3so.onrender.com/api
