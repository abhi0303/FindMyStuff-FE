/**
 * Small decorative stickers cut from the sticker sheet — transparent background,
 * no text. Purely decorative: render them with alt="" and keep meaning in the copy.
 */
import thumbsUp from './thumbs-up.webp';
import thinking from './thinking.webp';
import house from './house.webp';
import phone from './phone.webp';
import star from './star.webp';
import pin from './pin.webp';
import magnifier from './magnifier.webp';

export const stickers = {
  /** Boy giving a thumbs up — "everything is fine". */
  thumbsUp,
  /** Boy thinking, with question marks — "something needs a look". */
  thinking,
  /** Little house — places. */
  house,
  /** Phone with vibration lines — scanning a label. */
  phone,

  /* Small accents, meant to sit beside a main sticker. */
  star,
  pin,
  magnifier,
};
